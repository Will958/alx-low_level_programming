#include "main.h"
/**
 * _islower - checks lowercase character.
 * Return: 1 if letter is lowercase and 0 if not.
 */
int_islower(int c)
{
if (c >= 'a'; && c <= 'z')
{
return (1);
else
return (0);
}
}
