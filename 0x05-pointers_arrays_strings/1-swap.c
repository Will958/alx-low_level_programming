#include <stdio.h>
/**
 * swap_int - swaps the value of two integers
 * @a: first number
 * @b: second number
 * Return: nothing
 */
void swap_int(int *a, int *b)
int x, j;
{
x = *a;
y = *b;
*a = y;
*b = x;
}
